@model GestionProjects.Application.ViewModels.DemandeProjet.DemandeProjetEditViewModel
@inject GestionProjects.Application.Common.Interfaces.IPermissionService PermissionService

@{
    ViewData["Title"] = "Modifier la Demande";
    var isCorrection = Model.Demande.StatutDemande == GestionProjects.Domain.Enums.StatutDemande.CorrectionDemandeeParDirecteurMetier ||
                       Model.Demande.StatutDemande == GestionProjects.Domain.Enums.StatutDemande.RetourneeAuDemandeurParDSI;
    var canValidateDemandeAsDm = await PermissionService.CurrentUserHasPermissionAsync("DemandeProjet", "ValiderDM");
    var isDirecteurMetierEdit = canValidateDemandeAsDm &&
                                (Model.Demande.StatutDemande == GestionProjects.Domain.Enums.StatutDemande.EnAttenteValidationDirecteurMetier ||
                                 Model.Demande.StatutDemande == GestionProjects.Domain.Enums.StatutDemande.RetourneeAuDirecteurMetierParDSI);
    var hasMembresCodir = Model.HasMembresCodir;
    var cahierPresent = Model.Demande.CahierChargesDocument != null || !string.IsNullOrEmpty(Model.Demande.CahierChargesPath);
    var annexesExistantes = Model.Demande.DocumentsAnnexes.ToList();
    var pageTitle = isDirecteurMetierEdit ? "Modifier avant validation" : (isCorrection ? "Corriger la demande" : "Modifier la demande");
}

<div class="phase-shell">
    <section class="phase-hero">
        <div class="phase-hero-top">
            <div>
                <div class="phase-eyebrow">
                    <i class="bi bi-pencil-square"></i>
                    Demande projet
                </div>
                <h2 class="phase-title">@pageTitle</h2>
                <p class="phase-subtitle">
                    Ajustez le contenu fonctionnel, les priorités et les pièces jointes tout en conservant le circuit de validation métier.
                </p>
            </div>
            <div class="phase-action-row">
                <span class="phase-chip is-info">
                    <i class="bi bi-diagram-3"></i>
                    @Model.Demande.StatutDemande
                </span>
                @if (Model.Demande.DateSoumission != default)
                {
                    <span class="phase-chip">
                        <i class="bi bi-calendar3"></i>
                        @Model.Demande.DateSoumission.ToString("dd/MM/yyyy")
                    </span>
                }
            </div>
        </div>
    </section>

    @if (isCorrection)
    {
        <section class="phase-note is-warning">
            <div class="phase-strong">Correction demandée</div>
            <p style="margin: .6rem 0 0;">
                @(!string.IsNullOrWhiteSpace(Model.Demande.CommentaireDirecteurMetier) ? Model.Demande.CommentaireDirecteurMetier : Model.Demande.CommentaireDSI)
            </p>
        </section>
    }
    else if (isDirecteurMetierEdit)
    {
        <section class="phase-note is-info">
            <div class="phase-strong">Modification avant validation</div>
            <p style="margin: .6rem 0 0;">
                Vous pouvez ajuster la demande avant de la valider, de la rejeter ou de demander une correction.
            </p>
        </section>
    }

    <form asp-action="Edit" method="post" enctype="multipart/form-data">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="Demande.Id" />

        @await Html.PartialAsync("_ValidationSummary")

        <div class="phase-grid">
            <section class="phase-panel">
                <h3 class="phase-panel-title">Informations générales</h3>

                <div class="form-group-modern">
                    <label asp-for="Demande.Titre" class="form-label-modern">Titre du projet *</label>
                    <input asp-for="Demande.Titre" class="form-control-modern" required />
                    <span asp-validation-for="Demande.Titre" class="text-danger"></span>
                </div>

                <div class="form-group-modern">
                    <label asp-for="Demande.Description" class="form-label-modern">Description *</label>
                    <textarea asp-for="Demande.Description" class="form-control-modern" rows="4" required></textarea>
                    <span asp-validation-for="Demande.Description" class="text-danger"></span>
                </div>

                <div class="form-group-modern">
                    <label asp-for="Demande.Contexte" class="form-label-modern">Contexte *</label>
                    <textarea asp-for="Demande.Contexte" class="form-control-modern" rows="4" required></textarea>
                    <span asp-validation-for="Demande.Contexte" class="text-danger"></span>
                </div>

                <div class="form-group-modern">
                    <label asp-for="Demande.Objectifs" class="form-label-modern">Objectifs *</label>
                    <textarea asp-for="Demande.Objectifs" class="form-control-modern" rows="4" required></textarea>
                    <span asp-validation-for="Demande.Objectifs" class="text-danger"></span>
                </div>

                <div class="form-group-modern">
                    <label asp-for="Demande.AvantagesAttendus" class="form-label-modern">Avantages attendus *</label>
                    <textarea asp-for="Demande.AvantagesAttendus" class="form-control-modern" rows="3" required></textarea>
                    <span asp-validation-for="Demande.AvantagesAttendus" class="text-danger"></span>
                </div>

                <div class="form-group-modern">
                    <label asp-for="Demande.Perimetre" class="form-label-modern">Périmètre</label>
                    <textarea asp-for="Demande.Perimetre" class="form-control-modern" rows="4" placeholder="Processus impactés, activités couvertes, utilisateurs concernés..."></textarea>
                    <div class="soft-note">Décrivez le périmètre du projet : processus impactés, activités couvertes et population concernée.</div>
                </div>
            </section>

            <section class="phase-panel">
                <h3 class="phase-panel-title">Paramètres & gouvernance</h3>

                <div class="form-group-modern">
                    <label asp-for="Demande.DirectionId" class="form-label-modern">Direction *</label>
                    <select asp-for="Demande.DirectionId" id="DirectionId" class="form-control-modern" asp-items="Model.Directions.ToSelectListItems()" required>
                        <option value="">-- Sélectionner --</option>
                    </select>
                    <span asp-validation-for="Demande.DirectionId" class="text-danger"></span>
                </div>

                <div class="form-group-modern">
                    <label asp-for="Demande.DirecteurMetierId" class="form-label-modern">Directeur métier *</label>
                    <select asp-for="Demande.DirecteurMetierId" id="DirecteurMetierId" class="form-control-modern"
                            asp-items="Model.DirecteursMetier.ToSelectListItems()" required>
                        <option value="">-- Sélectionner d'abord une direction --</option>
                    </select>
                    <span asp-validation-for="Demande.DirecteurMetierId" class="text-danger"></span>
                    <div class="soft-note d-none" id="directeurMetierHelp">
                        <i class="bi bi-info-circle"></i>
                        Sélectionnez une direction pour voir les directeurs métier disponibles.
                    </div>
                </div>

                <div class="form-group-modern">
                    <label asp-for="Demande.AutreSponsorId" class="form-label-modern">Autre sponsor (CODIR)</label>
                    <select asp-for="Demande.AutreSponsorId" class="form-control-modern" asp-items="Model.AutresSponsors.ToSelectListItems()">
                        <option value="">-- Aucun autre sponsor --</option>
                    </select>
                    <span asp-validation-for="Demande.AutreSponsorId" class="text-danger"></span>
                    @if (hasMembresCodir)
                    {
                        <div class="soft-note">Sélectionnez, si besoin, un sponsor complémentaire parmi les membres du CODIR.</div>
                    }
                    else
                    {
                        <div class="soft-note text-warning">Aucun membre CODIR n'est encore paramétré dans l'administration.</div>
                    }
                </div>

                <div class="form-group-modern">
                    <label class="form-label-modern">Priorité *</label>
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label asp-for="Demande.Urgence" class="form-label-modern">Urgence</label>
                            <select asp-for="Demande.Urgence" class="form-control-modern" asp-items="Html.GetEnumSelectList<GestionProjects.Domain.Enums.UrgenceProjet>()" required></select>
                            <span asp-validation-for="Demande.Urgence" class="text-danger"></span>
                        </div>
                        <div class="col-md-6">
                            <label asp-for="Demande.Criticite" class="form-label-modern">Criticité</label>
                            <select asp-for="Demande.Criticite" class="form-control-modern" asp-items="Html.GetEnumSelectList<GestionProjects.Domain.Enums.CriticiteProjet>()" required></select>
                            <span asp-validation-for="Demande.Criticite" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="soft-note">La priorité du besoin est calculée à partir du couple urgence / criticité.</div>
                    <div id="prioriteCalculeePreview" class="phase-note is-info mt-3">
                        <div class="phase-strong">
                            Priorité calculée
                            <span id="prioriteCalculeeCode" class="badge-modern badge-modern-@Model.Demande.PrioriteBadgeClass ms-2">@Model.Demande.PrioriteCode</span>
                        </div>
                        <p id="prioriteCalculeeLibelle" style="margin: .6rem 0 0;">
                            @Model.Demande.PrioriteLibelle (score @Model.Demande.PrioriteScore/7)
                        </p>
                    </div>
                </div>

                <div class="form-group-modern">
                    <label asp-for="Demande.DateMiseEnOeuvreSouhaitee" class="form-label-modern">Date de mise en œuvre souhaitée</label>
                    <input asp-for="Demande.DateMiseEnOeuvreSouhaitee" type="date" class="form-control-modern" />
                </div>
            </section>
        </div>

        <section class="phase-panel">
            <div class="phase-section-header">
                <div>
                    <h3 class="phase-panel-title">Documents</h3>
                    <div class="phase-record-meta">Cahier des charges obligatoire, annexes complémentaires et lien vers le modèle standard.</div>
                </div>
            </div>

            @if (cahierPresent)
            {
                <article class="record-card">
                    <div class="record-card-header">
                        <div>
                            <div class="record-card-title">Cahier des charges actuel</div>
                            <div class="record-card-subtitle">Le fichier actuellement rattaché à la demande reste consultable tant qu'il n'est pas remplacé.</div>
                        </div>
                        <div class="record-card-actions">
                            <a asp-controller="Document"
                               asp-action="PreviewDemandeCahier"
                               asp-route-demandeId="@Model.Demande.Id"
                               target="_blank"
                               class="btn-modern btn-modern-primary"
                               title="Visualiser">
                                <i class="bi bi-eye"></i>
                            </a>
                        </div>
                    </div>
                </article>
            }

            <div class="phase-grid">
                <section class="phase-panel">
                    <div class="phase-section-header">
                        <div>
                            <h4 class="phase-panel-title">@(!cahierPresent ? "Cahier des charges" : "Nouveau cahier des charges")</h4>
                            <div class="phase-record-meta">Formats acceptés : PDF, DOC, DOCX.</div>
                        </div>
                        <a href="~/templates/Modele_Cahier_Charges_Projet_IT.docx" class="btn-modern btn-modern-secondary">
                            <i class="bi bi-download"></i>
                            Template
                        </a>
                    </div>

                    @if (!cahierPresent)
                    {
                        <input type="file" name="cahierCharges" class="form-control-modern" accept=".pdf,.doc,.docx" required />
                    }
                    else
                    {
                        <input type="file" name="cahierCharges" class="form-control-modern" accept=".pdf,.doc,.docx" />
                    }

                    @Html.ValidationMessage("cahierCharges", "", new { @class = "text-danger d-block mt-2" })
                    <div class="soft-note mt-2">Le cahier des charges reste obligatoire. Déposez un nouveau fichier uniquement pour le remplacer.</div>
                </section>

                <section class="phase-panel">
                    <h4 class="phase-panel-title">Annexes</h4>
                    @if (annexesExistantes.Any())
                    {
                        <div class="record-card-note">
                            <strong>Annexes existantes :</strong><br />
                            @annexesExistantes.Count fichier(s) déjà rattaché(s) à la demande.
                        </div>
                    }
                    <div class="form-group-modern">
                        <label class="form-label-modern">@(annexesExistantes.Any() ? "Nouvelles annexes" : "Annexes")</label>
                        <input type="file" name="annexes" class="form-control-modern" multiple accept=".pdf,.doc,.docx,.xls,.xlsx,.jpg,.png" />
                    </div>
                </section>
            </div>
        </section>

        <div class="phase-action-row">
            <button type="submit" class="btn-modern btn-modern-primary">
                <i class="bi bi-save"></i>
                @(isDirecteurMetierEdit ? "Enregistrer les modifications" : (isCorrection ? "Enregistrer les corrections" : "Enregistrer"))
            </button>
            <a asp-action="Details" asp-route-id="@Model.Demande.Id" class="btn-modern btn-modern-secondary">
                <i class="bi bi-x-circle"></i>
                Annuler
            </a>
        </div>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <script type="text/javascript">
        (function() {
            const directionSelect = document.getElementById('DirectionId');
            const directeurMetierSelect = document.getElementById('DirecteurMetierId');
            const directeurMetierHelp = document.getElementById('directeurMetierHelp');

            if (!directionSelect || !directeurMetierSelect) return;

            const currentDirecteurMetierId = directeurMetierSelect.value;

            function showHelp(visible) {
                if (!directeurMetierHelp) return;
                directeurMetierHelp.classList.toggle('d-none', !visible);
            }

            function loadDirecteursMetier(directionId, selectedId) {
                if (!directionId) {
                    directeurMetierSelect.innerHTML = '<option value="">-- Sélectionner d\\'abord une direction --</option>';
                    directeurMetierSelect.disabled = true;
                    showHelp(true);
                    return;
                }

                showHelp(false);
                directeurMetierSelect.innerHTML = '<option value="">-- Chargement... --</option>';
                directeurMetierSelect.disabled = true;

                fetch(`/DemandeProjet/GetDirecteursMetierByDirection?directionId=${directionId}`)
                    .then(response => {
                        if (!response.ok) {
                            throw new Error('Erreur lors du chargement des directeurs métier');
                        }
                        return response.json();
                    })
                    .then(data => {
                        directeurMetierSelect.innerHTML = '<option value="">-- Sélectionner --</option>';

                        if (data && data.length > 0) {
                            let defaultDirecteurId = null;

                            data.forEach(function(dm) {
                                const option = document.createElement('option');
                                option.value = dm.id;
                                option.textContent = dm.text;

                                if (dm.isDefault) {
                                    defaultDirecteurId = dm.id;
                                }

                                if (selectedId && dm.id === selectedId) {
                                    option.selected = true;
                                }

                                directeurMetierSelect.appendChild(option);
                            });

                            if (!selectedId && defaultDirecteurId) {
                                directeurMetierSelect.value = defaultDirecteurId;
                            }

                            directeurMetierSelect.disabled = false;
                        } else {
                            directeurMetierSelect.innerHTML = '<option value="">-- Aucun directeur métier trouvé pour cette direction --</option>';
                            directeurMetierSelect.disabled = true;
                        }
                    })
                    .catch(error => {
                        console.error('Erreur:', error);
                        directeurMetierSelect.innerHTML = '<option value="">-- Erreur lors du chargement --</option>';
                        directeurMetierSelect.disabled = true;
                    });
            }

            directionSelect.addEventListener('change', function() {
                loadDirecteursMetier(this.value, null);
            });

            if (directionSelect.value) {
                loadDirecteursMetier(directionSelect.value, currentDirecteurMetierId);
            } else {
                showHelp(true);
            }
        })();
    </script>
    <script type="text/javascript">
        (function() {
            const urgenceSelect = document.getElementById('Urgence');
            const criticiteSelect = document.getElementById('Criticite');
            const prioriteCode = document.getElementById('prioriteCalculeeCode');
            const prioriteLibelle = document.getElementById('prioriteCalculeeLibelle');

            if (!urgenceSelect || !criticiteSelect || !prioriteCode || !prioriteLibelle) {
                return;
            }

            function getPrioriteInfo(score) {
                switch (score) {
                    case 7:
                        return { code: 'P1', label: 'Critique', badge: 'danger' };
                    case 6:
                        return { code: 'P2', label: 'Très élevée', badge: 'warning' };
                    case 5:
                        return { code: 'P3', label: 'Élevée', badge: 'info' };
                    case 4:
                        return { code: 'P4', label: 'Modérée', badge: 'secondary' };
                    case 3:
                        return { code: 'P5', label: 'Faible', badge: 'secondary' };
                    default:
                        return { code: 'P6', label: 'Planifiée', badge: 'secondary' };
                }
            }

            function updatePrioritePreview() {
                const urgence = parseInt(urgenceSelect.value || '0', 10);
                const criticite = parseInt(criticiteSelect.value || '0', 10);

                if (!urgence || !criticite) {
                    prioriteCode.textContent = '-';
                    prioriteCode.className = 'badge-modern badge-modern-secondary';
                    prioriteLibelle.textContent = "Sélectionnez l'urgence et la criticité pour calculer la priorité.";
                    return;
                }

                const score = urgence + criticite;
                const priorite = getPrioriteInfo(score);

                prioriteCode.textContent = priorite.code;
                prioriteCode.className = 'badge-modern badge-modern-' + priorite.badge;
                prioriteLibelle.textContent = priorite.label + ' (score ' + score + '/7)';
            }

            urgenceSelect.addEventListener('change', updatePrioritePreview);
            criticiteSelect.addEventListener('change', updatePrioritePreview);
            updatePrioritePreview();
        })();
    </script>
}
